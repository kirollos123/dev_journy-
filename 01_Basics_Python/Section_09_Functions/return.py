def our_min( first ,secound):
    if first < secound:
        return first
    else:
        return secound
def our_max(first,secound):
    if first > secound:
        return first
    else:
        return secound
a,b=5,20
print('min=', our_min(a,b))
print('max=', our_max(a,b))